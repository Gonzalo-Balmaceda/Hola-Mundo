# Dada la siguiente tupla
nums = (13, 1, 8, 3, 2, 5, 8)
# Creamos una lista en la cual guardaremos los numeros menores a 5 y los mostramos en consola.
lista = []
for numeros in nums:
    if numeros < 5:
        lista.append(numeros) # Guardamos los numeros menores a 5 en lista.
        print(lista) # y los mostramos en la consola.""